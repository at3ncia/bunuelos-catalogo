[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: todas las rutas van a index.html para que React Router las maneje
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
